version: "3.13.5"

services:
  app:
    container_name: AP
    build:
      context: .
      dockerfile: Dockerfile
    command: python bot.py
    restart: on-failure
    ports:
      - "10:10"
      - "1010:1010"
